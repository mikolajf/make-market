[project]
name = "make-market"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
dev-dependencies = ["polylith-cli>=1.20.1"]

[tool.hatch.build]
dev-mode-dirs = ["components", "bases", "development", "."]
